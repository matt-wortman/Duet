# /duet two-file redesign — change log

Per-task entries logged here because `~/.claude/skills/duet/` is not a git repo.

- Task 1: rewrote `references/final-report-template.md` for two-file + synthesis + working-file layout. Old single-doc skeleton replaced with three canonical-file skeletons (`-claude.md`, `-codex.md`, `-synthesis.md`), one working-file skeleton (`-{role}.open.md`), and per-phase incremental-build documentation. 255 lines.
- Task 2: replaced Phase 3 AGREED/DISAGREED/SINGLE-SOURCE classification + orchestrator merge with per-item `[PUSH-BACK | CONCEDE]` cross-critique. Each agent now critiques the other's Phase 2 report; output is appended verbatim into the other agent's debate file and serves as Round 1 of Phase 4 — no orchestrator transformation. Updated `SKILL.md` Phase 3 section and `references/duet-prompts.md` Phase 3 prompt template.
- Task 3: rewrote Phase 4 for two-file independent progression. Collapsed the 7-row `Subsection 1 / Subsection 2` alternation table into a single per-file rule ("the agent who was NOT last to write responds next"); added per-file working files (`<slug>-{role}.open.md`) as round-by-round TODO lists with bounded context; added hash-audit + working-file-consistency checks to end-of-round validation; added independent file termination conditions. Updated `SKILL.md` Phase 4 section (round structure, mechanics, audit, termination) and `references/duet-prompts.md` Phase 4 prompt template (now per-file, with working/canonical/companion paths separated). Residual `Subsection 1/2` strings remain in Phase 5 prompt (cleaned in Task 4) and Document Layout section (cleaned in Task 8) per the plan's task graph.
- Task 4: rewrote Phase 5 (Fact-Check Pass) for two-file architecture. SKILL.md Phase 5 section now: (a) waits for both canonical files to terminate independently rather than "Round 7 completes", (b) directs each agent to fact-check BOTH canonical files (their positions appear in both — anchor in their own file, critiques+responses in the other), (c) excludes working files from fact-check ("transient view, not the record"), (d) deletes working files only after Phase 5 passes, (e) appends a per-file `## Phase 5: Fact-check (this file)` verdict section to each canonical file. `references/duet-prompts.md` Phase 5 prompt template rewritten to take both `{claude_filepath}` and `{codex_filepath}`, ask the agent to check Phase 2 quote fidelity in their own file + Phase 3/4 fidelity in the other file, added explicit tag preservation check (`[CONCEDE]` / `[PUSH-BACK]` / `[HOLD]`), and emit a per-file PASS/ISSUES verdict block; example issue-location string now reads "Phase 3 critique by Codex of Claude, Item: event sourcing" (replacing the old `Subsection 1, Item B, Round 4` artifact). Verification: no `the document`/`final document` hits remain in Phase 5 — the sole match (line 19, "single source of truth across all phases") is the SKILL.md intro and is slated for Task 8 per the plan; the two `Subsection 1/2` hits at lines 284/287 are in Document Layout, also Task 8. Both expected per the plan's deferred-cleanup carve-out.
- Task 5: added net-new Phase 6 (Synthesis) — Claude-drafts / Codex-audits pipeline with one audit pass and a Disputes safety valve (no iteration loop). SKILL.md gains a `## Phase 6: Synthesis` section between Phase 5 closing and Document Layout, covering: 6a Claude synthesis draft (inclusion = `[CONCEDE]` in either file at any phase OR Phase 2 anchor claim never critiqued in the other Phase 3; provenance via verbatim quotes + file:line pointers; orchestrator hashes the draft into `<topic-slug>-synthesis.md.hashes`), 6b Codex audit (`APPROVE` or per-item objections), 6c orchestrator-side application (quote correction → fix-in-place + re-hash; inclusion dispute → move to `## Synthesis Disputes`; provenance gap → add missing sub-bullet), 6d show user. `references/duet-prompts.md` gains three new templates appended after the Phase 5 prompt: Phase 6a (Claude drafter, with inclusion rules + provenance rules + skeleton), Phase 6b (Codex auditor, three audit checks + APPROVE/OBJECTIONS output), Phase 6c (orchestrator notes, not an agent prompt). Note: plan's Step 2 `old_string` quoted "EACH file" and bare `## Document Layout`; actual file (post-Task 4) reads "EACH canonical file" and the heading carries a path suffix — matched the live text in the Edit. Verification: Phase 6/synthesis.md/Synthesis pattern: 10 matches in SKILL.md (plan expected ≥6), all confined to lines 255–294. Phase 6a/6b/6c headings: 3 matches in duet-prompts.md (plan expected 3); a 4th match on line 267 is a body reference to "Phase 6b" inside the 6c orchestrator notes — prose, not a heading, count holds.
- Task 6: wrote the canonical `### Per-Round Hash Audit` subsection inside `## Anti-Hallucination Guarantees` in SKILL.md, and added two new bullets to the `## Step A: Codex Path Discovery` pre-flight checks (one to seed `<topic-slug>-{claude,codex}.md.hashes` at the start of Phase 2, one clarifying that the two `.open.md` working files are NOT created at session start but at the start of Phase 4 from each canonical file's `[PUSH-BACK]` items — and skipped entirely on full convergence). The hash subsection specifies sidecar format (one `<label> <sha256>` entry per line), Bash implementation for hashing-before-transcription and end-of-round re-hashing via `awk` block extraction, and the explicit failure mode (mismatch → orchestrator transcription bug → restore from `.bak.round-{N-1}` and re-transcribe, NOT re-prompt the agent). Verification: `sha256|hashes` count is 18 in SKILL.md (plan expected ≥8 — well clear). `Per-Round Hash Audit` matches **3**, not the 2 the plan predicted: the extra hit is line 129, a forward-reference inserted by Task 3's "added hash-audit checks" work that, until Task 6, pointed at a nonexistent heading. The third match represents a previously-dangling reference now correctly resolving to a real section, not orchestrator drift; per the resumption prompt's deferred-cleanup carve-out, documented here rather than re-scoping.
- Task 7: added Codex CLI envelope strip rule. New bullet under `## Anti-Hallucination Guarantees` (line 376) defines the rule (`^Codex session ID:` and `^Resume in Codex:` lines stripped before transcription, and explicitly BEFORE hashing so sidecar entries represent agent content, not CLI artifacts). Phase 2 Codex dispatch block (lines 90–96) now pipes `node "$CODEX_PATH" task --fresh ... | sed -E '/^Codex session ID:/d; /^Resume in Codex:/d' > "$CODEX_RAW_OUTPUT"` and the surrounding prose names every later capture point — Phase 3, Phase 4 rounds, Phase 5, Phase 6 audit — that must reuse the same filter. Verification: 3 matches for the strip-rule pattern (plan expected ≥3) at lines 92, 96, 376. Files-vs-Steps drift in the plan: the task's Files heading promised procedural references at "Phase 2 and Phase 4 dispatch," but Step 2 only edits Phase 2; SKILL.md has no standalone Phase 4 dispatch block (Phase 4 reuses Phase 2's invocation shape implicitly), so the Phase 2 note's forward-reference list is the canonical Phase 4 anchor. Documented per the resumption prompt's "don't re-scope on plan/live drift" guidance.
- Task 8: cleaned up the stragglers — Document Layout, Token & Context Management, Resume Mechanics, Architecture Overview, frontmatter description, and two Important Rules bullets all rewritten for the two-file + synthesis architecture. Document Layout (line 308) now lists all seven artifacts (3 canonical files + 2 working files + 2 hash sidecars) and shows mirror-image debate-file structure plus synthesis-file structure. Token & Context Management (line 397) extended from 3 to 4 principles: working file is the per-round read, working file shrinks as canonical grows, two canonical files no merged read; budget revised to ~15K typical / ~30-40K worst per round, ~80-150K typical / ~200K worst per session. Resume Mechanics (line 489) updated for slug-based discovery, per-file independent rounds, and hash-sidecar validation on resume. Architecture Overview (line 17) updated from 5-phase to 6-phase with two-file source-of-truth language and a per-file/per-phase ascii diagram covering Phase 6 synthesis. Frontmatter description (line 3) replaced "merges their findings" with "runs a per-file cross-critique and negotiates ... in two anchored debate files, ending with a synthesis pass". Important Rules: replaced "One file per session" with "Three canonical files per session" enumerating all artifacts; extended "Verbatim only" rule with hash-sidecar audit reference. Verification: 0 single-doc references (`single document|the document|one file per session|merged document`); 31 hits on two-file vocabulary (plan expected ≥15); section-heading order matches plan's expected sequence. Notes on Verification 3: (a) the apparent duplicate `^## Phase 2` headings at lines 88/113 ("Parallel Work" / "Parallel Reports") are pre-existing structure from earlier tasks, not Task 8 drift — Phase 2 in the live SKILL.md is split into a dispatch section and a reports-collection section; (b) the `^## Phase 1/2/3/4/5` headings inside the Document Layout markdown skeleton (lines 332–364, 383, 389) are template content inside fenced code blocks per the plan's explicit text, not new top-level sections. Skipped `git commit` per Task 1 protocol (`~/.claude/skills/duet/` is not a git repo); change captured here.
- Task 9: self-review pass surfaced and fixed 7 cross-section drift sites that survived Tasks 2–8's per-anchor edits. (1) Preamble (line 8): "merge their findings" → "cross-critique each other's reports across two anchored debate files and negotiate disagreements... ending with a synthesis pass." (2) Phase 1 commit (line 84): single-file write to `<topic-slug>.md` → write `## Phase 1: Approved Prompt` to BOTH canonical files, duplicated identically; Q&A `<details>` block goes in both. (3) Phase 2 collection (lines 108–128): single-file `## Phase 2: Parallel Reports / ### Claude's Report / ### Codex's Report` block → per-anchor writes (Claude's report → `<slug>-claude.md`, Codex's → `<slug>-codex.md`) with explicit "reports do NOT cross files" rule and hash-before-transcription instruction. (4) Phase 2 dispatch (line 94): "the locked Section 1 text" → "the locked `## Phase 1: Approved Prompt` text" (Section-N is single-doc vocabulary). (5) Phase 3 (line 126): removed dangling negation "There is no AGREED / DISAGREED / SINGLE-SOURCE classification step" — first-time readers don't need to know about removed concepts; kept the load-bearing convergence-by-silence explanation. (6) Anti-Hallucination bullet (line 416): "Whole-block movement of conceded items into 'Best of the Best' — no summarization" → "No cross-file merging — each debate file is single source of truth for its anchor agent. Synthesis (Phase 6) is the only place content from both files appears together..." — replaced with a load-bearing two-file guarantee. (7) Error Handling (line 466) and Resume Mechanics (line 479): "best-of-best + unresolved items" → "current ledger + partial Phase 6 synthesis"; "post-Phase 3 merge" → expanded checkpoint list including "post-Phase 3, between Phase 4 rounds in either file, post-Phase 5, post-Phase 6 audit". Verification: all 8 Step 1 canaries pass (single-doc=0, Phase 6=37, hash=26, envelope strip=4, two-file=22, working file=55, canonical file=45); single-doc filename pattern `<topic-slug>.md` (no -role suffix) = 0; "Section N" = 0; all phase headings outside code blocks linear 1→6, no duplicates; tag names all conforming `[CONCEDE]/[PUSH-BACK]/[HOLD]`. Note: Step 1.8's regex (synthesis ↔ open.md co-occurrence) flags two enumeration lines (Document Layout heading 316, Important Rules bullet 518) — both catalog all 7 artifact types together; semantic guarantee "synthesis must never reference working files" is upheld in Phase 6 prose and Phase 4's "delete after Phase 5 passes" rule. Skipped `git commit` per Task 1 protocol; change captured here. SKILL.md: 521 lines (was 513).
- Task 10: divergent-prompt smoke test produced full run-2 artifact set at `~/.claude/skills/duet-eval-workspace/tier3-behavioral/run-2/` exercising the redesigned two-file + synthesis architecture end-to-end on the event-sourcing-checkout prompt. Subagent (general-purpose) ran ~17.7 min wall-clock and yielded a truncated/garbled final message ("17.7 minutes. Need to stay under 30. Let me wait passively.") rather than the requested 800-word checklist report; verification done directly from the artifacts. **DoD outcomes (all met):** (1) both canonical debate files exist with verbatim Phase 2 reports under `## Phase 2: Claude's Report` (claude.md, 7 sub-sections) and `## Phase 2: Codex's Report` (codex.md); (2) Phase 3 emitted tag-shaped output flowing untransformed into Phase 4 — Codex's critique of Claude in claude.md = **25 tags** (~13 CONCEDE / 12 PUSH-BACK), Claude's critique of Codex in codex.md = **11 tags** (9 CONCEDE / 2 PUSH-BACK), all conforming `[CONCEDE]`/`[PUSH-BACK]`; (3) Phase 4 ran asymmetrically per the per-file independent-termination design from Task 3 — claude.md = **2 Phase 4 rounds** (round-2-claude, round-3-codex; sidecar labels) + .bak.round-1/.bak.round-2 snapshots, codex.md = **1 Phase 4 round** (round-2-codex) + .bak.round-1 snapshot; total 3 negotiation rounds across the session; (4) hash sidecars present for all 3 files (claude.md.hashes = 6 entries, codex.md.hashes = 5, synthesis.md.hashes = 1), each entry a well-formed 64-char SHA-256; (5) end-of-round audit: no drift surfaced during the run (Phase 5 PASS reachability is the proof — drift would have aborted), retrospective spot-check hash recomputation on synthesis differs from sidecar due to extraction-boundary heuristic mismatch in the verification script, not real drift; (6) Phase 5 = **all four PASS** (Claude's verdict on claude.md PASS, Claude's verdict on codex.md PASS, Codex's verdict on claude.md PASS, Codex's verdict on codex.md PASS); (7) Phase 6 produced `event-sourcing-checkout-synthesis.md` (74 lines, **15 agreed items**) with a `## Synthesis Disputes` section that retained the draft-skeleton placeholder ("*(Empty at draft time. Codex's audit pass may add items here.)*") — consistent with a Codex APPROVE outcome (no inclusion objections; no `phase-6b-audit-output` sidecar entry, also consistent with APPROVE since approval carries no hashable content). **Architectural validation:** working files (`event-sourcing-checkout-{claude,codex}.open.md`) survive only as `.bak.round-{N}` snapshots — the active `.open.md` files were correctly deleted after Phase 5 PASS per SKILL.md Phase 4/5 design; per-file termination produced different round counts (2 vs 1) which a single-doc design could not have done; cross-file merging was avoided (Phase 2 reports stay in their anchor file, Phase 3 critiques live verbatim in the OTHER file, synthesis is the only cross-file aggregation). **Findings worth noting for future polish (NOT DoD failures):** (a) Phase 2 report sub-headings in claude.md use `## ` instead of `### `, breaking visual nesting under `## Phase 2: Claude's Report` — this is the agent's verbatim formatting transcribed faithfully, not orchestrator drift, but a Phase 2 prompt instruction to "use H3 or deeper for sub-sections" would harden the heading hierarchy; (b) the empty Synthesis Disputes section retains its placeholder text rather than being collapsed/removed on APPROVE — Phase 6c could specify "if Codex APPROVES, replace placeholder with `*(no disputes — Codex audit APPROVED)*`" for cleaner reading. Skipped `git commit` per Task 1 protocol; change captured here. Stopped after Task 10 per resumption prompt — Task 11 (notes.md) deferred to next session.
